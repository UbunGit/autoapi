'use strict';

module.exports = class Autoapi {

    constructor(opts = {}) {
        console.log("Autoapi.constructor.opts:", opts)
    }
    test() {
        // this.db = createDatabaseManager();
        console.log("Autoapi.constructor.test")
    }
}
